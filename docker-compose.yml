services:
  landing-page:
    container_name: landing-page
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    external: true
